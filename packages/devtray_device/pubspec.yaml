name: devtray_device
description: "Real device facts for devtray's Device page — model, OS and app version, via device_info_plus and package_info_plus."
version: 0.6.0
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_device
issue_tracker: https://github.com/hassan171/devtray/issues


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.6.0
  device_info_plus: ">=11.0.0 <13.0.0"
  package_info_plus: ">=8.0.0 <10.0.0"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
