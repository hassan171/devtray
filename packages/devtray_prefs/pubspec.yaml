name: devtray_prefs
description: "shared_preferences support for devtray — a Storage page adapter, and persistence for mock rules."
version: 0.3.0
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_prefs
issue_tracker: https://github.com/hassan171/devtray/issues


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.3.0
  shared_preferences: ^2.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
