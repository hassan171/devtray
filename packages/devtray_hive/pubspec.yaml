name: devtray_hive
description: "Hive support for devtray's Storage page — register a box as you open it, and browse or edit it at runtime."
version: 0.6.2
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_hive
issue_tracker: https://github.com/hassan171/devtray/issues
topics:
  - debugging
  - devtools
  - hive
  - storage


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.6.2
  hive_ce: ^2.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
