name: devtray_dio
description: "dio adapter for devtray — feeds the overlay's network inspector from a Dio client."
version: 0.5.0
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_dio
issue_tracker: https://github.com/hassan171/devtray/issues


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.5.0
  dio: ^5.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
