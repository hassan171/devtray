name: devtray_http
description: "package:http adapter for devtray — feeds the overlay's network inspector from an http Client."
version: 0.6.0
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_http
issue_tracker: https://github.com/hassan171/devtray/issues


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.6.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
