name: devtray_html
description: "HTML response preview for devtray's Network page — renders a server-rendered page instead of dumping its markup."
version: 0.6.1
repository: https://github.com/hassan171/devtray/tree/main/packages/devtray_html
issue_tracker: https://github.com/hassan171/devtray/issues


environment:
  sdk: ">=3.12.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  devtray: ^0.6.1
  flutter_html: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
