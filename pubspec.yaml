name: debug_overlay
description: "A draggable in-app debugging overlay with a pluggable page system and a built-in network inspector (dio + http adapters)."
version: 0.1.0
homepage:

environment:
  sdk: ^3.12.2
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  http: ^1.2.0
  flutter_html: ^3.0.0
  device_info_plus: ^11.0.0
  package_info_plus: ^8.0.0
  shared_preferences: ^2.3.0
  bloc: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
